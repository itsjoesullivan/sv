# static web server

## Install

Two steps:

  - Ensure that $GOPATH/bin is in your path:

```bash
# ~/.bash_profile
PATH=$PATH:$GOPATH/bin
```

  - <code>go get</code> it:

```bash
go get github.com/itsjoesullivan/sv
```

## Use it

```bash
sv
```
